'use client';
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';
import {courseUnits,difficultyLabels,type Difficulty} from '@/lib/game-settings';
export default function SettingsPicker({course,unit,difficulty,onUnit,onDifficulty,disabled=false}:{course:string;unit:string;difficulty:Difficulty;onUnit:(s:string)=>void;onDifficulty:(s:Difficulty)=>void;disabled?:boolean}){return <div className="settings-picker"><label>출제 단원<Select value={unit} onValueChange={onUnit} disabled={disabled}><SelectTrigger className="course-select"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">{course==='수학 종합'?'6개 과목 골고루':'전체 단원'}</SelectItem>{(courseUnits[course]??[]).map(u=><SelectItem key={u} value={u}>{u}</SelectItem>)}</SelectContent></Select></label><label>난이도<Select value={difficulty} onValueChange={v=>onDifficulty(v as Difficulty)} disabled={disabled}><SelectTrigger className="course-select"><SelectValue/></SelectTrigger><SelectContent>{Object.entries(difficultyLabels).map(([v,label])=><SelectItem key={v} value={v}>{label}</SelectItem>)}</SelectContent></Select></label><p>{disabled?'참여 코드가 있으면 선생님의 수업방 설정을 따릅니다.':{basic:'핵심 개념과 직접 계산부터 차근차근',standard:'조건을 해석하고 배운 개념을 적용',advanced:'여러 조건과 개념을 결합하여 해결'}[difficulty]}</p><p>오지선다형 3문항 + 주관식 3문항{course==='수학 종합'?' · 공통수학1·2, 대수, 미적분Ⅰ, 확률과 통계, 기하에서 각 1문항':''}</p></div>}
